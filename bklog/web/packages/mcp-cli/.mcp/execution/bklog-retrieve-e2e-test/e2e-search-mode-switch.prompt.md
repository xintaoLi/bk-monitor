# 搜索模式切换测试

作为测试工程师，请测试搜索模式切换功能：

1. 打开检索页面并等待加载完成
2. 确认当前处于 UI 模式（默认）
3. 点击模式切换按钮，切换到 SQL 模式
4. 验证搜索栏变为 SQL 输入模式
5. 输入 SQL 查询语句：log: error
6. 执行搜索
7. 再次点击模式切换，切回 UI 模式
8. 验证搜索栏恢复为 UI 模式

**可用 test-id**：
- mode-switch: 模式切换按钮
- v3-search-bar: 搜索栏

**预期结果**：UI/SQL 模式切换正常，搜索功能在两种模式下均可用