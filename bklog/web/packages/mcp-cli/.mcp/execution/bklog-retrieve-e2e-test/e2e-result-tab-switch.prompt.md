# 结果Tab切换测试

作为测试工程师，请测试结果展示区域的 Tab 切换功能：

1. 打开检索页面并执行一次搜索
2. 确认当前处于「原始日志」Tab
3. 点击「日志聚类」Tab（如果可用）
4. 验证日志聚类视图正常显示
5. 点击「图表分析」Tab（如果可用）
6. 验证图表分析视图正常显示
7. 点击「Grep模式」Tab（如果可用）
8. 验证 Grep 模式视图正常显示
9. 点击「原始日志」Tab 返回

**可用 test-id**：
- search-result-tab: 结果 Tab 组件

**预期结果**：所有可用的 Tab 切换正常，对应视图正确渲染